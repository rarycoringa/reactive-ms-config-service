services:
  config:
    build: .
    container_name: config
    environment:
      - SERVER_PORT=8888
      - GITHUB_ACCESS_TOKEN=${GITHUB_ACCESS_TOKEN}
    ports:
      - "8888:8888"
    networks:
      - reactive-ms-network

networks:
  reactive-ms-network:
    external: true
